services:
  - type: web
    name: telegram-sketch-bot
    env: python
    region: frankfurt
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
    autoDeploy: true
    plan: free