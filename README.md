# 🤖 Telegram Sketch to Render Bot

Бот для преобразования эскизов комнат в фотореалистичные рендеры с помощью ComfyUI.

## 🚀 Быстрый старт

### 1. Настройка на вашем ПК
```bash
# Клонируйте репозиторий
git clone <ваш-репозиторий>
cd telegram-sketch-bot

# Установите зависимости
python -m venv venv
venv\Scripts\activate  # Windows
pip install -r requirements.txt

# Создайте .env файл
cp .env.example .env
# Отредактируйте .env файл